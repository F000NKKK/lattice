[package]
name = "lattice-backend-linux"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
homepage.workspace = true
documentation.workspace = true
categories.workspace = true
keywords.workspace = true
description = "Linux backend for Lattice: implements lattice-platform's provider traits via Netlink."

[target.'cfg(target_os = "linux")'.dependencies]
lattice-core = { workspace = true }
lattice-ip = { workspace = true }
lattice-model = { workspace = true }
lattice-platform = { workspace = true }
rtnetlink = { workspace = true }
tokio = { workspace = true, features = ["rt-multi-thread"] }
futures = { workspace = true }
